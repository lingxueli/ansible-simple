ansible local control node setup

# 1. inventory, a list of hosts

# 2. roles, the modules used in playbook to carry out functions

# 3. cfg file, set up ansible in the control node

# 4. playbook: yaml file


installation guide
https://docs.ansible.com/ansible/latest/index.html
er45re
$ ansible --version

execution location


$ ansible myservers -m ping
$ ansible localhost -m ping

$ ansible localhost -m find -a "paths=Downloads file_type=file"



$ ansible-playbook first.yml
$ ansible-playbook first.yml -v